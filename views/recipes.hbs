<h2 id="category-name">All recipes</h2>

<ul class="category-list">
</ul>

<div class="recipe-area">
    {{!-- {{#each this.datas}}
    <div class="row recipe">
        {{#each this}}
        {{> recipe_card this}}
        {{/each}}
    </div>
    {{/each}} --}}

    <div class="row recipe">
        {{#each this.datas}}
        {{> recipe_card this}}
        {{/each}}
    </div>
    <button type="button" class="btn btn-secondary btn-lg btn-block more">View more</button>
</div>